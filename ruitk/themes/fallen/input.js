import { Merge } from "../../support/merger.js";
import { Style } from "../../support/style.js";

let elements = [
    // elements have been moved to staging while a full implmention is worked on.
];

export class FallenInput {
    static getElements() {
        return elements;
    }
}
